package course.s4_ExpreStateCBMethodsAndMore;

public class p4_IfThenElseRecap {
    public static void main(String[] args) {

    }

    // The if statement identifies which statement or code block to run based on the value of
    // an expression. In other words based on a specific condition.

    // Inside the code block defined by curly braces
    // { and } we can have one or multiple statements.

    // We can use the else statement after the if. In that case when the condition is false the
    // else block will be executed.

    // We can also add else if to test multiple conditions.

    // Basic structure of if then else statement:
    // if (condition) {
        // if statement (block)
        // if condition is true, block inside of if statement will run else block inside else statement.
    // } else {
        // else statement (block)
    // }
}
